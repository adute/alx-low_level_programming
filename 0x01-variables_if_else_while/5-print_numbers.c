#include <stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 (success)
 *
 */
int main(void)
{
	int singleDigit = 0;

	while (singleDigit < 10)
	{
		printf("%d", singleDigit);
		singleDigit++;
	}
	printf("\n");

return (0);

}

#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	const char *s = "\"Programming is like building a multilingual puzzle";

	puts(s,\n);
	return (0);
}

This directory consists of more singly linked lists of functions:-

prints all the elements of listint_t

returns the number of elements in the list

adds new element to the list

adds new element at the end of the list

function that frees list

free and set head to null

deletes the head node of list and returns the head node data

returns the nth node of a linked list

the summ of all data(n) of linked list

inserts new nodw at a given position

delets node at index of linked list

and four more advanced tasks.
